# `forcefill.checks`

The output-side checks: build an `openmm.System` from the generated XML, and optionally minimize it.

```{eval-rst}
.. automodule:: forcefill.checks
   :members:
   :undoc-members:
   :show-inheritance:
```
