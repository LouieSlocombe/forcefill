# `forcefill.topology`

Reading the input structure: which residues the base force field cannot match, why each one was triaged the way it was, and how a single residue is extracted for parameterization.

```{eval-rst}
.. automodule:: forcefill.topology
   :members:
   :undoc-members:
   :show-inheritance:
```
