# User guide

The long-form half of the documentation: what forcefill will and will not
parameterize, the four backends and when each one applies, and the checks it runs
on both sides of the expensive step.

Read {doc}`../quickstart` first if you have not — everything here assumes the
shape of a `build_forcefield_xml` call.

```{toctree}
:maxdepth: 2

what-gets-parameterized
cleaning
ligands
backends
charmm
bespokefit
checks
gotchas
openmmforcefields
```
